# CODEBUDDY.md —— 临床速查助手 · 疾病速查极简决策卡片规范

> 本文件是项目级指令。所有「疾病速查」条目的新增与重构，都必须严格遵循以下守则。
> 数据存放于 `data/demo.json`，每条疾病用 `card` 字段承载 Markdown 决策卡片；正文渲染由 `js/app.js` 的 Markdown 引擎解析。

---

## 一、核心设计哲学：【脱水精炼 · 弹性积木 · 拒绝注水 · 指南权威】

- **速查卡片属性**：疾病速查不是教科书，必须文字极度精炼、字数克制、一眼抓取重点。单篇总字数一般控制在 **400～1000 字**（复杂疑难病不超 1200 字），坚决剔除长篇机理与文字堆砌。
- **杜绝冗余 Emoji**：标题和正文**严禁滥用 Emoji 表情符号**（如 📋 🔍 🧪 💊 🚨 💡 等），保持严肃专业的临床指南质感。
- **杜绝开发技术术语**：正文中不得出现“严格左右对齐”、“卡片模式”等给开发者看的设计与实现说明。

## 二、标题与排版规约

- **标题格式**：统一采用 `# 疾病名称（英文名称）`。
- 解析引擎已原生支持小字号副标，括号内的英文会自动渲染为浅灰、小字号（0.52em）副标，**严禁在 Markdown 源码中手写 `<small>`、`<span>` 等 HTML 标签**。
- **大纲分级**：一级大模块统一用 `## 模块名称`；正文细项优先使用加粗标签列表 `- **类别**：内容`，避免使用过多深层 `###` 产生碎片化留白。

## 三、模块结构：【四大核心必选 + 弹性按需选填】

### 必须包含的 4 大核心模块

1. **标题与一句话概述**
   - `# 疾病名（英文名）` 下方紧跟 1 句话干练定义，讲明病因、核心临床特征与主要危害。

2. **`## 诊断要点`**
   - 精炼诊断金标准、核心评估依据及**重型高危人群**。

3. **`## 常用药物参考`（高密度速查表格）**
   - 凡有药物治疗的疾病，**一律采用 3 列 Markdown 表格**呈现（`| 适用情形 / 分类 | 推荐药物与用法 | 核心临床说明 |`）；
   - 表格内文字干练，严禁大段抄写说明书，只写关键药物名、规格剂量、关键时机与核心禁忌；
   - 纯外科手术类疾病，表格转为“围手术期用药与镇痛抗感染”。

4. **`## 住院 / 转诊提示`**
   - 守住临床安全底线，精炼列出急重症报警指标、收治住院标准或转上级医院指征。

### 因病制宜的“弹性选填”模块（坚决按需裁减，不生搬硬套，严禁凑数）

- **`## 临床表现`（按需）**：症状显著的疾病，列 2~3 项单行精炼要点；早期隐匿无症状的慢病 1 句话带过或合并入诊断要点，**不为了凑格式而硬写**。
- **`## 鉴别诊断`（按需）**：易混淆、易误诊疾病列出 2~4 个关键鉴别病种与核心鉴别点；体征单一、诊断极其明确的疾病**坚决省略**或 1 句话带过。
- **`## 推荐检查`（按需）**：仅列出该病的**决定性检查**与**金标准**，避免堆砌血尿常规等无意义套话。
- **`## 治疗原则`（按需）**：分层救治原则、非药物干预基石、抗菌药物/激素把关红线。
- **`## 临床提示`（按需精炼，拒绝凑数）**：有几条写几条，直击临床命门（用药绝对禁忌、致命陷阱、真伪假阴性辨别）；**绝对禁止为凑 5~6 条写“定期复查、遵医嘱、保持心情舒畅”等注水话**。
- **`## 参考指南`（按需选填，置于文末）**：
  - 非必填；编写/重构时若参考了具体国家指南、专家共识或规范，可列 1~3 条权威出处；
  - **严禁编造出处**：只写真实存在、可核验的指南/共识（名称+年份/卷期须与事实相符）；拿不准、原文无出处、罕见病无专病指南时**坚决省略**，不得占位虚构；
  - 前端引擎已原生适配灰度小字号与虚线分割。

---

## 四、本工程落地约定（实现侧，勿写入卡片正文）

- **数据**：`data/demo.json` 中每个疾病条目含 `id / name / abbr / category / tab:"疾病速查" / summary / card`。`card` 为符合上述模块的 Markdown 字符串；`abbr` 为拼音首字母（搜索用）。
- **新增方式**：
  - 单条手动新增：直接编辑 `data/demo.json`，在对应 `tab` 数组追加一条，写全 `card`（按本规范）；同时把同一份数据同步进 `js/app.js` 的 `FALLBACK_DATA`（保证双击 `index.html` 可用）。
  - 批量导入教材：运行 `build_diseases.py`，从《内科学》md 抽取并自动生成 `card`（仅机械重组已有文本，缺失的「常用药物参考 / 住院转诊 / 参考指南」宁可省略，绝不编造）。
- **渲染**：`js/app.js` 的 `renderDetail()` 优先渲染 `card` 字段；Markdown 引擎原生支持 `# 标题英文副标`(灰小字)、`## 模块`、加粗标签列表、`| 表格 |`、以及 `> 参考指南`(灰小字+虚线分割)。
- **安全红线**：任何卡片内容不得编造药物剂量、转诊指征或指南出处；来源缺失的模块一律省略，宁缺毋滥。
